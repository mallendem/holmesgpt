# Toolset configuration for Confluence URL lookup test
toolsets:
  confluence:
    enabled: true
  # Disable K8s-dependent toolsets (no K8s in this test)
  kubernetes/core:
    enabled: false
  kubernetes/logs:
    enabled: false
  helm/core:
    enabled: false
